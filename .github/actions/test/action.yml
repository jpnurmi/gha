name: Test
description: Pretends to run tests
inputs:
  os:
    type: string
    required: false
  container:
    type: string
    required: false

runs:
  using: composite

  steps:
    - run: echo "Hello"
      shell: bash
