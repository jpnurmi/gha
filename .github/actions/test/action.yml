name: Test
description: Pretends to run tests
inputs:
  duration:
    type: number
    required: true

runs:
  using: composite

  steps:
    - name: Dummy outdated checkout
      uses: actions/checkout@v4

    - run: sleep ${{ inputs.duration }}
      shell: bash
